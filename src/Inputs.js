import React, { Component } from "react";
import { Panel } from "react-bootstrap";

export default class Inputs extends Component {
  render() {
    return (
      <div>
        <Panel header="Inputs">
          this will contain the inputs needed to make a model
        </Panel>
      </div>
    );
  }
}
